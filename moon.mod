name = "justjavac/num_cpus"

version = "0.1.9"

readme = "README.md"

repository = "https://github.com/justjavac/moonbit-num-cpus"

license = "MIT"

keywords = [ "cpu", "cpus", "core", "processor", "hardware", "system" ]

description = "Get the number of CPU cores available on the system."

preferred_target = "native"

supported_targets = "+native"

options(
  source: "src",
)
